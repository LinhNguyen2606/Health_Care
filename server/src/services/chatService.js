import db from '../models/index';

let userChats = () => {
    return new Promise(async (resolve, reject) => {
        try {
            // let conversation = await db.Conversation.findAll({userId: })
        } catch (e) {}
    });
};

module.exports = {
    userChats: userChats,
};
